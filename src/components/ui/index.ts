/**
 * Tamagui-based reusable UI primitives.
 * These replace common RN patterns with compiler-optimized Tamagui components.
 */

export { XStack, YStack, Text, styled, Separator, Input, Button, ScrollView, View } from 'tamagui';
